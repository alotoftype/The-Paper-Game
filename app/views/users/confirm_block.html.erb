<% title 'Block "' + @user.login + '"?'%>

<h1>Block "<%= @user.login %>"?</h1>

Do you want to block this user?

<%= button_to 'Yes, they annoy me!', block_user_path(@user), :method => :post %>
<%= button_to 'No, go back!', back_or_default_path(user_path @user), :method => :get %>