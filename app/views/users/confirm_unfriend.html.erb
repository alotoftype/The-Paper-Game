<% title 'Unfriend "' + @user.login + '"?'%>

<h1>Unfriend "<%= @user.login %>"?</h1>

Do you want to remove this user from your friends list?

<%= button_to 'Yes, we''re no longer friends!', unfriend_user_path(@user), :method => :post %>
<%= button_to 'No, go back!', back_or_default_path(user_path @user), :method => :get %>